# API secrets (set via `wrangler secret put` for production)
# DeepL Free API - https://www.deepl.com/your-account/keys
DEEPL_API_KEY=

# Frontend (only needed for local dev if API is on a different host)
VITE_API_URL=
